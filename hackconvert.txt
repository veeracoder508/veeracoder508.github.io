<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Binary Converter</title>
    <style>
        body {
            font-family: monospace;
            background-color: #000;
            color: #0f0;
            margin: 20px;
        }

        #container {
            border: 2px solid #0f0;
            padding: 20px;
            background-color: rgba(0, 0, 0, 0.8);
        }

        label {
            display: block; /* Labels on separate lines */
            margin-bottom: 5px;
        }

        input[type="text"] {
            width: calc(100% - 20px); /* Full width, accounting for padding */
            padding: 8px;
            margin-bottom: 10px;
            background-color: #000;
            color: #0f0;
            border: 1px solid #0f0;
        }

        button {
            padding: 8px 16px;
            background-color: #000;
            color: #0f0;
            border: 1px solid #0f0;
            cursor: pointer;
        }

        #result {
            margin-top: 10px;
            font-weight: bold;
        }

        /* Matrix Rain Effect (Optional) */
        #matrix {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .drop {
            position: absolute;
            background-color: #0f0;
            width: 1px;
            height: var(--length);
            animation: rain var(--speed) linear infinite;
        }

        @keyframes rain {
            0% { transform: translateY(-100vh); }
            100% { transform: translateY(100vh); }
        }
    </style>
</head>
<body>

    <h1>Binary Converter</h1>

    <div id="container">
        <label for="decimalInput">Decimal:</label>
        <input type="text" id="decimalInput">

        <button onclick="convertDecimalToBinary()">Convert to Binary</button>

        <label for="binaryInput">Binary:</label>
        <input type="text" id="binaryInput">

        <button onclick="convertBinaryToDecimal()">Convert to Decimal</button>

        <div id="result"></div>
    </div>

    <div id="matrix"></div>

    <script>
        function convertDecimalToBinary() {
            const decimalInput = document.getElementById("decimalInput");
            const binaryInput = document.getElementById("binaryInput");
            const decimal = parseInt(decimalInput.value);

            if (isNaN(decimal)) {
                displayResult("Invalid decimal input.");
                return;
            }

            const binary = decimal.toString(2);
            binaryInput.value = binary;
            displayResult(""); // Clear any previous messages
        }

        function convertBinaryToDecimal() {
            const decimalInput = document.getElementById("decimalInput");
            const binaryInput = document.getElementById("binaryInput");
            const binary = binaryInput.value;

            if (!/^[01]+$/.test(binary)) { // Check for valid binary (0s and 1s only)
                displayResult("Invalid binary input.");
                return;
            }


            const decimal = parseInt(binary, 2);
            decimalInput.value = decimal;
            displayResult(""); // Clear any previous messages
        }

        function displayResult(message) {
            const resultDiv = document.getElementById("result");
            resultDiv.textContent = message;
        }

                // Matrix Rain Effect (Optional)
        const matrix = document.getElementById("matrix");
        const numDrops = window.innerWidth / 10; // Adjust density
        for (let i = 0; i < numDrops; i++) {
            const drop = document.createElement("div");
            drop.className = "drop";
            drop.style.left = `${i * 10}px`; // Horizontal position
            drop.style.setProperty("--length", `${Math.random() * 100}vh`); // Random length
            drop.style.setProperty("--speed", `${Math.random() * 2 + 1}s`); // Random speed
            matrix.appendChild(drop);
        }
    </script>

</body>
</html>
